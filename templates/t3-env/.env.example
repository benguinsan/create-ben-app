# Environment variables validated by env.ts (T3 Env + Zod)
#
# Copy to `.env.local` and fill in values.
# Never commit real secrets. Only NEXT_PUBLIC_* values reach the browser.
#
# NODE_ENV is set automatically by Next.js during dev/build — you usually do not
# need to set it here.
